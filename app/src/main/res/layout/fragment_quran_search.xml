<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".ui.quran.search.fragments.QuranSearchFragment">

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/search_card_background"
            android:id="@+id/quran_search_et"
            android:hint="@string/search_with_aya"
            android:padding="@dimen/spacing_too_small"
            android:layout_margin="@dimen/spacing_small"
            android:drawablePadding="@dimen/spacing_too_small"
            android:drawableStart="@drawable/baseline_search_24" />

        <androidx.recyclerview.widget.RecyclerView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:listitem="@layout/quran_search_item"
            android:id="@+id/search_recycler" />

    </LinearLayout>
</layout>